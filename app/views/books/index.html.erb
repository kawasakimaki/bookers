<h1>Books</h1>

<th>Title</th>
<th>Body</th>

<% @books.each do |book| %>
  <%= book.title %>
  <%= book.body %>

  <%= link_to 'Show', book_path(book.id) %>
  <%= link_to 'Edit', "/books/:id/edit" %>
  <%= link_to 'Destroy', "/books/:id" %>

<% end %>